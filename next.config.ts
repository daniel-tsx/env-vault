import type { NextConfig } from "next";

// Baseline security headers. No Content-Security-Policy here on purpose: a strict
// CSP would need a nonce for next-themes' pre-paint inline script; that's left as
// a follow-up rather than risking a broken theme flash.
const securityHeaders = [
  { key: "X-Frame-Options", value: "DENY" },
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  {
    key: "Strict-Transport-Security",
    value: "max-age=63072000; includeSubDomains; preload",
  },
];

const nextConfig: NextConfig = {
  async headers() {
    return [{ source: "/:path*", headers: securityHeaders }];
  },
};

export default nextConfig;
